# Server Configuration
server.port=9091

# Spring Application Configuration
spring.application.name=review-service

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/reviewdb
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update 

# Session Configuration
spring.session.jdbc.initialize-schema=always

# Eureka Instance Configuration
# This ensures the service registers with its IP address, solving the UnknownHost issue
eureka.instance.prefer-ip-address=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# JWT Configuration
jwt.secret=Y2Y4ZTM1YmYtYjYyMC00ZDllLTlhZTMtZDY2ZDU4ZTMxZmE5Cg==